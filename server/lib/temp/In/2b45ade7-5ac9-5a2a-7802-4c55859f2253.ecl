IMPORT DataPatterns;#OPTION('OUTPUTLIMIT',2000);recLayout := RECORD  string salesorganisation;  string product;  string product_group;  string distribution_channel;  string month;  integer8 quality;  real8 revenue; END;inputds_4h44 := DATASET('~thor::sachin::samplesalesdata',recLayout, THOR);OUTPUT(inputds_4h44, NAMED('dataPatternsName_qwkt'));rawProfileResults := DataPatterns.Profile(inputds_4h44,fieldListStr:= '', maxPatterns:=100, maxPatternLen:= 100,features := 'cardinality,best_ecl_types,modes', sampleSize := 100);OUTPUT(rawProfileResults,,'thor::sachin::samplesale_datapattern.thor', OVERWRITE);